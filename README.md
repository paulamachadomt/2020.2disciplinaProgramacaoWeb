## Primeiro teste com Spring Boot
Atividade para mostrar criação de um projeto com Spring Boot (framework Java). 

Controller que faz imprimir no navegador (http://localhost:8080) a típica mensagem "Hello, world!".
